app.controller('shareController', ['$scope', '$state', '$http', '$filter', '$localStorage', '$modal', '$window',function($scope,$state, $http, $filter, $localStorage, $modal, $window) {
    $scope.confirm = function(){
        alert('敬请期待');
    };
}]);